
main() {
  
  final lista = const ['Ana', 'Lia', 'Gui']; //indico que a lista é final, podendo apenas inserir coisas dentro da lista, nao sobreescrevela, porem se atribuir const aos elentos da lista, logo os valores tambem não podem ser alterados.
  lista.add('Rebeca'); 
  print(lista);

  // lista = ['Banana', 'Maça'];
}